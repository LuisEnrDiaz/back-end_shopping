import { CartI } from '../types/cart/cartI';

export const mockDataCart = {
    products: [''],
    user: '1',
    id: '1',
};

export const mockDataCart2: Partial<CartI> = {
    products: [],
};
